// Auth + admin-setup streaming fallback. Single centered card on a dark canvas.
"use client";

import { AuthRouteSkeleton } from "@/components/ui/skeleton";

export function AuthLoading() {
  return <AuthRouteSkeleton />;
}
